import { StyleSheet } from 'react-native';

const Styles = StyleSheet.create({
  container: {
    flex: 1,
  },

  list: {
    alignContent: 'center',
    alignSelf: 'center',
  },
});

export default Styles;
